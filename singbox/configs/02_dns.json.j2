{
  "dns": {
    "servers": [
        {
        "tag": "cloudflare",
        "address": {% if hconfigs['dns_server'] %} {{ hconfigs['dns_server'] }} {% else %} "1.1.1.1" {% endif %},
        "strategy": "prefer_ipv4",
        "detour": "freedom"
      }
    ],
    "rules": [],
    "final": "cloudflare",
    "strategy": "prefer_ipv4"
  }
}